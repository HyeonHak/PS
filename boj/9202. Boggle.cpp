#include <iostream>
#include <cstring>
#include <string>
using namespace std;

int main(void)
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    int w;
    string str;

    cin >> w;
    for (int i = 0; i < w; i++)
    {
        cin >> str;
    }
    
    return (0);
}